# Example environment variables for local development

# Server Configuration
PORT=8443
HEALTH_PORT=8080
SKIP_TLS=true  # Set to false in production

# Registry timeout in milliseconds (default: 240000 = 4 minutes)
REGISTRY_TIMEOUT=240000

# TLS Certificate paths (when SKIP_TLS=false)
TLS_CERT_PATH=./certs/tls.crt
TLS_KEY_PATH=./certs/tls.key

# Target Registry - Where images should be cloned to
# If set, the webhook checks if images exist in this registry (instead of source)
# This is useful to verify if images have already been cloned/mirrored
# Format: registry-host (no protocol, no trailing slash)
# Example: myregistry.azurecr.io
TARGET_REGISTRY=

# Registry Authentication Option 1: Docker config.json format
# DOCKER_CONFIG_JSON={"auths":{"docker.io":{"username":"user","password":"pass"}}}

# Registry Authentication Option 2: Individual registries
# REGISTRY_DOCKERHUB_URL=docker.io
# REGISTRY_DOCKERHUB_USERNAME=your-username
# REGISTRY_DOCKERHUB_PASSWORD=your-password

# REGISTRY_GHCR_URL=ghcr.io
# REGISTRY_GHCR_USERNAME=your-github-username
# REGISTRY_GHCR_TOKEN=ghp_xxxxxxxxxxxxx

# REGISTRY_GCR_URL=gcr.io
# REGISTRY_GCR_USERNAME=_json_key
# REGISTRY_GCR_PASSWORD={"type":"service_account",...}

# Default credentials (used when no specific match)
# DEFAULT_REGISTRY_URL=docker.io
# DEFAULT_REGISTRY_USERNAME=your-username
# DEFAULT_REGISTRY_PASSWORD=your-password
