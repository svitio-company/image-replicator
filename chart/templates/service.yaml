apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-validator-webhook.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "image-validator-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "image-validator-webhook.selectorLabels" . | nindent 4 }}
